import absolutePulse from './absolute_pulse';
import relativePulse from './relative_pulse';
import intervalPulse from './interval_pulse';

export default {
  absolutePulse: absolutePulse,
  relativePulse: relativePulse,
  intervalPulse: intervalPulse
};
