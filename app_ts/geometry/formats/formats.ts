import { SquareFormat } from './square.format';
import { RectangleFormat } from './rectangle.format';
import { CircleFormat } from './circle.format';

export var formats = {
  SquareFormat: SquareFormat,
  RectangleFormat: RectangleFormat,
  CircleFormat: CircleFormat
}
